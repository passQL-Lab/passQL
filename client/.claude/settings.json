{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; echo \"$f\" | grep -qE '\\.(ts|tsx|js|jsx)$' && cd /Users/luca/Documents/GitHub/passQL/client && npx tsc --noEmit 2>&1 | tail -5 || true; }",
            "timeout": 30,
            "statusMessage": "TypeScript typecheck..."
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/luca/Documents/GitHub/passQL/client && npm run build 2>&1 | tail -10 && npx vitest run 2>&1 | tail -10",
            "timeout": 120,
            "statusMessage": "Build + Test pipeline..."
          }
        ]
      }
    ]
  }
}
