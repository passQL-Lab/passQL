# Home Page Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Prompt1_Home.md 스펙대로 홈 화면 UI를 mock 데이터 기반으로 구현한다. API 연동은 별도 이슈.

**Architecture:** Home.tsx를 4개 섹션(Greeting, TodayQuestion, StreakBadge, ProgressCards)으로 구성한다. 각 섹션은 인라인으로 작성하되, 이후 컴포넌트 추출 가능하도록 JSX를 논리적으로 분리한다. 모든 데이터는 mock 상수로 정의하고 나중에 API로 교체한다.

**Tech Stack:** React 19, Tailwind CSS 4 tokens, 기존 디자인 시스템 CSS 클래스

---

## File Structure

| Action | Path | Responsibility |
|--------|------|---------------|
| Rewrite | `src/pages/Home.tsx` | 홈 화면 전체 (4개 섹션, mock 데이터) |

---

### Task 1: Home 페이지 구현

**Files:**
- Rewrite: `src/pages/Home.tsx`

- [ ] **Step 1: Home.tsx 전체 교체**

Prompt1 스펙의 4개 섹션을 모두 구현한다:
1. Greeting (기하학적 아바타 + 닉네임)
2. 오늘의 문제 카드 (4px 인디고 좌측 보더, 지문 미리보기, chevron)
3. 스트릭 뱃지 pill (🔥 연속 N일)
4. 통계 카드 2개 (푼 문제 수 + 정답률 프로그레스 바)

```tsx
// Mock data — API 연동 시 교체
const MOCK_NICKNAME = "용감한 판다";
const MOCK_INITIALS = "용판";
const MOCK_TODAY_QUESTION = "고객별 주문 수를 구하는 올바른 SQL은?";
const MOCK_STREAK = 3;
const MOCK_SOLVED = 42;
const MOCK_CORRECT_RATE = 68;

export default function Home() {
  return (
    <div className="py-6 space-y-0">
      {/* 1. Greeting */}
      <section className="flex items-center gap-3 mb-8">
        <div
          className="flex-shrink-0 w-10 h-10 rounded-full flex items-center justify-center text-white font-bold text-sm"
          style={{ backgroundColor: "var(--color-brand)" }}
        >
          {MOCK_INITIALS}
        </div>
        <h1 className="text-h2">안녕하세요, {MOCK_NICKNAME}</h1>
      </section>

      {/* 2. 오늘의 문제 */}
      <section className="mb-4">
        <div className="card-base flex items-center gap-4 border-l-4 border-l-brand cursor-pointer hover:bg-surface transition-colors">
          <div className="flex-1 min-w-0">
            <p className="text-secondary mb-1">오늘의 문제</p>
            <p className="text-body truncate">{MOCK_TODAY_QUESTION}</p>
          </div>
          <span className="text-text-caption text-lg flex-shrink-0">›</span>
        </div>
      </section>

      {/* 3. 스트릭 뱃지 */}
      <section className="mb-6">
        <span
          className="inline-flex items-center rounded-full px-3 py-1 text-sm font-bold"
          style={{
            backgroundColor: "var(--color-sem-warning-light)",
            color: "var(--color-sem-warning-text)",
          }}
        >
          🔥 연속 {MOCK_STREAK}일
        </span>
      </section>

      {/* 4. 통계 카드 2개 */}
      <section className="grid grid-cols-2 gap-3">
        {/* 푼 문제 */}
        <div className="card-base flex flex-col items-start">
          <span className="text-h1 text-brand">{MOCK_SOLVED}</span>
          <span className="text-secondary mt-1">푼 문제</span>
        </div>

        {/* 정답률 */}
        <div className="card-base flex flex-col items-start">
          <span className="text-h1 text-brand">{MOCK_CORRECT_RATE}%</span>
          <span className="text-secondary mt-1">정답률</span>
          <div className="w-full mt-2 h-1 rounded-full bg-border">
            <div
              className="h-full rounded-full bg-brand"
              style={{ width: `${MOCK_CORRECT_RATE}%` }}
            />
          </div>
        </div>
      </section>
    </div>
  );
}
```

**스펙 체크리스트:**
- ✅ 기하학적 아바타: 40px 인디고 원 + 흰색 이니셜 "용판"
- ✅ 닉네임: Pretendard 22px bold #111827 (`text-h2`)
- ✅ 32px bottom margin (mb-8)
- ✅ 오늘의 문제: white card, 4px #4F46E5 좌측 보더, "오늘의 문제" 라벨 14px #6B7280, 지문 16px 1줄 truncate, chevron-right
- ✅ hover: subtle #FAFAFA background shift
- ✅ 스트릭 pill: #FEF3C7 bg, #D97706 text, bold, "🔥 연속 3일"
- ✅ 카드 1: "42" 28px bold #4F46E5 + "푼 문제" 14px #6B7280
- ✅ 카드 2: "68%" 28px bold #4F46E5 + "정답률" + progress bar (4px, rounded-full, 68% filled #4F46E5 / remaining #E5E7EB)
- ✅ 카드 gap: 12px (`gap-3`)
- ✅ Korean text throughout, no illustrations

- [ ] **Step 2: 빌드 확인**

Run: `npm run build`

- [ ] **Step 3: Commit**

```bash
git add src/pages/Home.tsx
git commit -m "feat: 홈 화면 UI 구현 (mock 데이터) #9"
```
